import React from "react"
import { Button } from "@react-md/button"

import "./style.scss"

function button() {
  return (
    <Button id="headerButton" theme="primary" themeType="contained">
      Los geht's
    </Button>
  )
}

export default button
